This is an attempt at a (relatively) high performance tensor library, utilizing expression templates and variadic templating from the C++11 standard. The goal is to produce a multi-dimensional array interface that is as syntactically simple to use as possible without sacrificing on performance.
